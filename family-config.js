// DRANVI FAMILY public API endpoint.
// When the local server is exposed through a tunnel (e.g. Cloudflare Tunnel),
// set the public URL here so guardians can write logs from dranvi.com.
// Empty string = same origin only (writes work only on localhost).
window.DRANVI_FAMILY_API = 'https://family-api.dranvi.com';
